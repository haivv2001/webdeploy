// const imageContainer = document.querySelector('.image');
// const image = imageContainer.querySelector('img');

// // Set the initial rotation
// image.style.transform = 'rotate(0deg)';

// // Start the animation
// function animate() {
//   const currentRotation = parseFloat(image.style.transform.split('rotate(')[1].split(')')[0]);
//   const newRotation = currentRotation + 1; // Rotate 1 degree per frame

//   image.style.transform = `rotate(${newRotation}deg)`;

//   requestAnimationFrame(animate); // Request the next animation frame
// }

// animate();
// function rotateImage() {
//     var image = document.querySelector('.img');
//     image.classList.toggle('img-right'); // Thêm hoặc loại bỏ lớp rotate-right để xoay ảnh
// }